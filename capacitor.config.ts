import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.etm.v3111',
  appName: 'ETM Module v3.1.1.26',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;